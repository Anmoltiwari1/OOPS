package Packages;
//packages--> are the container for classes
//         -->it is just a folder 
//          -->Simply we cannot create same name .java file in same folder(package) so we use different folder(package).
public class packages {
    
}
